import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ca2e55",
    },
    secondary: {
      main: "#7f7eff",
      light: "#808782",
    },
  },
});

export default theme;
