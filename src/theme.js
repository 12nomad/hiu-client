import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#011936",
      light: "#2274a5",
    },
    secondary: {
      main: "#e83f6f",
    },
  },
});

export default theme;
